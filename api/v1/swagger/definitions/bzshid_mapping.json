{
  "type": "object",
  "properties": {
    "localId": {
      "type": "integer",
      "description": "Local ID (biblionumber) of the bib record"
    },
    "bszshId": {
      "type": "string",
      "description": "BZSH catalog ID of the library network catalog (ZKSH)"
    }
  },
  "additionalProperties": false,
  "required": ["localId", "bszshId"]
}
